extends layout

block content
  .navbar-fixed
    nav
      .nav-wrapper(style='background-color: black; padding-left:2%')
          a.brand-logo(style='font-family:"Lato", sans-serif')(href='#!') Mess Data Retrieval System
  .p
     This Project will help in those areas where the outgoing and incoming of people
     is more than the total capacity , For example If in a Mess the total capacity is 800 and there are 1000 students enrolled to that mess and due to infrastructure constraints one can not extend the capacity. We have designed a webapp that will let you know the real time person count data of the mess and will help you deciding the optimal time to go to the mess to avoid chaos and to save time.

  .row
    .col.s.m4.l4
      .card-panel.grey.lighten-5.z-depth-1
        .row
          p People taking lunch at mess at this time:
          p(style='font-family: "Lato", monospace;')
            each post, i in posts
              span
                strong #{post.totalCountofpeople}
              //- if posts.timeStamp
    .row
    .col.s12.m4.l4
      .card-panel.grey.lighten-5.z-depth-1
        .row
          p Time analysys pie chart
          p This feature will be up once we collect dataset
    .row
    .col.s12.m4.l4
      .card-panel.grey.lighten-5.z-depth-1
        .row
          p Net capacity of Mess(500).
          p(style='font-family: "Lucida Console", Monaco, monospace')
            each post, i in posts
              span #{post.totalCount}

      // Import jQuery before materialize.js
      script(type='text/javascript', src='https://code.jquery.com/jquery-2.1.1.min.js')
      script(type='text/javascript', src='js/materialize.min.js')
      script.
        $('.datepicker').pickadate({
        selectMonths: true, // Creates a dropdown to control month
        selectYears: 100 // Creates a dropdown of 15 years to control year
        });
      script.
        $(document).ready(function() {
        $('select').material_select();
        });
